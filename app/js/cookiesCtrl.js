
dinnerPlannerApp.controller('CookiesCtrl', ['$scope', '$cookieStore', function($scope, $cookieStore) {
    // Retrieving a cookie
    // var sample = $cookieStore.get('type');
    // console.log(favoriteCookie);
    // // Setting a cookie
    // $cookieStore.put('type', 'cookie');
}]);
